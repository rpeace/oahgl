<h1>New team</h1>
<% if current_user.player_type == captain && current_user.team == nil %>
	<%= render 'form' %>
<% else %>
	<p>In order to create a team, you need to be a captain with 

<%= link_to 'Back', teams_path %>
